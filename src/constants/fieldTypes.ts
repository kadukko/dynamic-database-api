const FIELD_TYPES = {
  STRING: 'string',
  NUMBER: 'number',
  BOOLEAN: 'boolean',
  OBJECT_ID: 'objectId',
};

export const FIELD_TYPE_VALUES = Object.values(FIELD_TYPES);

export default FIELD_TYPES;
